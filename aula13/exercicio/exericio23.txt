1 - A tag <body> é a tag que indica que o corpo do html foi iniciado, e fechando o corpo html </body>
2 - Não, nem todos elementos possuem definição de margin e padding
3 - Sim, já que a tag <figure> tem como objetivo advertir semanticamente que existem figuras(imagens), e que essas figuras podem ter ou não lengenda(<figcaption>)
